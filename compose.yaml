services:
  accounting:
    build:
      network: host
      context: .
    ports:
      - 9095:9095
      - 5005:5005
    networks:
      - accounting
    volumes:
      - ./accounting_data:/data

networks:
  accounting:
    driver: host